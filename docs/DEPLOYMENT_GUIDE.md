# 🚀 Deployment Guide on Ubuntu VPS

1. Update system: `sudo apt update && sudo apt upgrade -y`
2. Install Python: `sudo apt install python3-pip python3-venv git -y`
3. Clone repo and run `pip install -r requirements.txt`
4. Setup systemd service for 24/7 background execution.
